# --- FastAPI Core ---
fastapi==0.110.0
uvicorn[standard]==0.29.0

# --- HTTP Clients (to talk to Rasa server) ---
httpx==0.27.0
requests==2.31.0

# --- Databases ---
motor==3.3.2               # Async MongoDB client
psycopg2-binary==2.9.9     # PostgreSQL adapter
redis==5.0.3               # Redis client (use 4.5.4 if Rasa compatibility needed)

# --- Data validation & config ---
pydantic==1.10.13
python-dotenv==1.0.1

# --- CORS (Cross-Origin Resource Sharing) ---
fastapi[all]

# --- Optional for fuzzy matching ---
fuzzywuzzy==0.18.0
python-Levenshtein==0.23.0

