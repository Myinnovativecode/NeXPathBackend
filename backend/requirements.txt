# --- FastAPI Core ---
fastapi==0.110.0
uvicorn[standard]==0.29.0

# --- HTTP Clients (for external API calls, LLMs etc.) ---
httpx==0.27.0
requests==2.31.0

# --- Databases ---
motor==3.3.2               # Async MongoDB client
psycopg2-binary==2.9.9     # PostgreSQL adapter
redis==5.0.3               # Redis client

# --- Data validation & config ---
pydantic==2.9.2
pydantic-settings==2.5.2   # replacement for Pydantic v2 BaseSettings
python-dotenv==1.0.1

# --- Optional for fuzzy matching / string similarity ---
fuzzywuzzy==0.18.0
python-Levenshtein==0.23.0




